import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
appId: 'com.kingstore.app',
appName: 'KING STORE',
webDir: 'dist',
server: {
androidScheme: 'https'
}
};
export default config;
